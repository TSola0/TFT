# 引脚分配与接线说明

> 适用：STM32F103C8T6 最小系统板（Blue Pill）开发验证阶段  
> 后续打板：可直接沿用本分配，只需重新布局连接器位置

## 1. 核心原则

- 沿用当前 `TFT.ioc` 已配好的资源：
  - `SPI1` → TFT 高速 SPI 数据
  - `USART1` → ASRPRO 语音模块
  - `USART2` → 调试串口（USB-TTL）
- 新增 3 个 GPIO 控制 TFT：`CS`、`DC`、`BL`
- **SWD 调试口 PA13/PA14 必须保留**，不要随便占用
- **CubeMX 里 SYS → Debug 必须选 `Serial Wire`**，当前是 `No Debug`，会导致 ST-Link 无法下载/调试

## 2. 引脚分配总表

| 功能 | STM32 引脚 | 方向 | 连接对象 | 备注 |
|---|---|---|---|---|
| SPI1_SCK | **PA5** | 输出 | TFT SCL / CLK | 已配置 |
| SPI1_MISO | **PA6** | 输入 | TFT SDO | 已配置，开发验证可悬空 |
| SPI1_MOSI | **PA7** | 输出 | TFT SDI / MOSI | 已配置 |
| TFT_CS | **PA4** | 输出 | TFT CS | 新增 `GPIO_Output`，默认高电平 |
| TFT_DC | **PA8** | 输出 | TFT D/C | 新增 `GPIO_Output`，默认高电平 |
| TFT_BL | **PB1** | 输出 | TFT BLK | 新增 `GPIO_Output`，默认高电平；后续可改 PWM |
| USART1_TX | **PA9** | 输出 | ASRPRO RX | 已配置 |
| USART1_RX | **PA10** | 输入 | ASRPRO TX | 已配置 |
| USART2_TX | **PA2** | 输出 | USB-TTL RX | 调试用 |
| USART2_RX | **PA3** | 输入 | USB-TTL TX | 调试用 |
| SWDIO | **PA13** | — | ST-Link SWDIO | 保留，不要接其他外设 |
| SWCLK | **PA14** | — | ST-Link SWCLK | 保留，不要接其他外设 |
| 状态 LED | **PC13** | 输出 | 板载 LED | 可选，低电平点亮 |
| HSE 晶振 | **PD0 / PD1** | — | 8MHz 晶振 | CubeMX 已固定，不动 |

## 3. 杜邦线接线图（开发板 → 外设）

### 2.8 寸 ILI9341 TFT 模块（8P）

```text
TFT 模块          Blue Pill
VCC  ──────────── 5V          (模块自带 1117-3.3 稳压，接 5V 更稳)
GND  ──────────── GND
SDI  ──────────── PA7  (MOSI)
SCL  ──────────── PA5  (SCK)
CS   ──────────── PA4
SDO  ──────────── PA6  (MISO)  可不接
D/C  ──────────── PA8
BLK  ──────────── PB1
```

### ASRPRO 语音模块（典型 4P）

```text
ASRPRO        Blue Pill
VCC  ──────── 3.3V
GND  ──────── GND
RX   ──────── PA9  (USART1_TX)
TX   ──────── PA10 (USART1_RX)
```

> 注意：STM32 的 TX 接模块的 RX，RX 接模块的 TX，别接反喵。

### USB-TTL 调试器（CH340 / CP2102）

```text
USB-TTL       Blue Pill
RX   ──────── PA2  (USART2_TX)
TX   ──────── PA3  (USART2_RX)
GND  ──────── GND
```

### ST-Link V2 调试器

```text
ST-Link       Blue Pill
SWDIO ─────── PA13
SWCLK ─────── PA14
GND   ─────── GND
3.3V  ───○    (不接！目标板由 USB 供电)
```

> 如果目标板没接 USB，可以把 ST-Link 的 3.3V 接到 Blue Pill 的 3.3V 供电；**但不能同时接 USB 5V 和 ST-Link 3.3V**，会冲突喵。

## 4. CubeMX 需要补的 3 件事

1. **打开 `TFT.ioc`**
2. **新增 3 个 GPIO_Output**：
   - `PA4` → User Label: `TFT_CS`
   - `PA8` → User Label: `TFT_DC`
   - `PB1` → User Label: `TFT_BL`
3. **SYS → Debug 改为 `Serial Wire`**（现在可能是 `No Debug`）
4. 这 3 个 GPIO 的配置建议：
   - **Mode**: `GPIO_Output`
   - **GPIO output level**: `High`
   - **GPIO mode**: `Output Push Pull`
   - **GPIO Pull-up/Pull-down**: `No pull-up and no pull-down`
   - **Maximum output speed**: `Low`
5. **SPI1 速度**：开发板验证阶段保持 `Prescaler = /4`（18 MHz）更稳；点屏成功后再改成 `/2`（36 MHz）测上限喵。
6. **Project → Generate Code**，生成后 `lcd.h` 里的 `#define` 无需改动（已经对应 PA4/PA8/PB1）。

## 5. 开发板常见坑

| 现象 | 原因 | 处理 |
|---|---|---|
| 点不亮 / 白屏 | 背光没开 | `PB1` 输出高，或直接把 `BLK` 接到 3.3V 测试 |
| ST-Link 识别不到 | `SYS → Debug = No Debug` | 回 CubeMX 改成 `Serial Wire` 重新生成 |
| 下载失败 | BOOT0 跳线帽位置不对 | 确认 BOOT0 = 0（接 GND） |
| 串口乱码 | USB-TTL 与 STM32 不共地 | 必须接 GND |
| 颜色反了 / 镜像 | ILI9341 MADCTL 方向位不对 | 调 `LCD_SetRotation()` 里的 `0x36` 参数 |

## 6. 后续打板建议

- **TFT 座子**：8P 1.25mm 或 2.0mm 卧贴，线序与模块完全一致，方便直接插线。
- **ASRPRO 座子**：4P（3.3V / GND / RX / TX），RX/TX 交叉引出。
- **调试口**：至少引出 SWDIO / SWCLK / GND 三个测试点，方便量产烧录。
- **电源**：USB 5V 输入 → AMS1117-3.3 给 STM32 和 ASRPRO；TFT 可单独由 5V 直供（利用模块自带稳压），降低 3.3V 轨压力。
